spring:
  application:
    name: house-service

  datasource:
    url: jdbc:postgresql://localhost:49799/house_serviceDB
    username: postgres
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect